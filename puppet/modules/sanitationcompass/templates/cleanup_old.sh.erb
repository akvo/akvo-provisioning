#!/bin/bash

VERSIONS_DIR=<%= @approot %>/versions/

if [[ -z "$1" ]]
then
    REMOVE_COUNT=4
else
    REMOVE_COUNT=$1
fi

for x in `ls -t ${VERSIONS_DIR} | tail -n +${REMOVE_COUNT}`
do
    echo "Removing old version: $x"
    rm -r $VERSIONS_DIR/$x
done
