#!/bin/bash

VERSIONS_DIR=<%= @approot %>/versions/

# by default we want to keep four old versions
if [[ -z "$1" ]]
then
    REMOVE_COUNT=4
else
    REMOVE_COUNT=$1
fi

for x in `ls -t ${VERSIONS_DIR} | awk 'NR>'$REMOVE_COUNT`
do
    echo "Removing old version: $x"
    rm -r $VERSIONS_DIR/$x
done
