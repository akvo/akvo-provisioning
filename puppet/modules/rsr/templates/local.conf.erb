
LOG_FILE_PATH='<%= @logdir %>'

SECRET_KEY='secret_key_todo_change_this'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'rsr',
        'USER': 'rsr',
        'PASSWORD': '<%= @database_password %>',
        'HOST': '<%= @database_host %>',
        'PORT': '',
    }
}

MEDIA_ROOT='<%= @media_root %>'

RSR_DOMAINS=("<%= @rsr_domain %>",)

PARTNER_SITES_DOMAINS=("localdev.akvo.org")


DEBUG=True
