
LOG_FILE_PATH='<%= @logdir %>'

SECRET_KEY='<%= @rsr_secret_key %>'

PARTNER_SITES_MARKETING_SITE = 'http://www.akvoapp.org/'

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'rsr',
        'USER': 'rsr',
        'PASSWORD': '<%= @database_password %>',
        'HOST': '<%= @database_host %>',
        'PORT': '',
    }
}

MEDIA_ROOT='<%= @media_root %>'

DEBUG=True

RSR_SITE_REGEXPS = [
    <% @rsr_hostnames.each do |rsr_hostname| -%>
        '^<%= rsr_hostname.gsub('.', '\\.') %>$',
    <% end -%>
]

PARTNER_SITE_REGEXPS = [
    <% @partner_hostnames.each do |partner_hostname| -%>
        '^<%= partner_hostname.gsub('.', '\\.') %>$',
    <% end -%>
]
