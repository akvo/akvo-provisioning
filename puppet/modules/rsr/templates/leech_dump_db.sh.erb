#!/bin/bash

USERNAME=<%= @username %>
PASS=<%= @database_password %>
HOST=<%= @database_host %>
DATABASE=<%= @username %>
OUTFILE=$1

PGPASSWORD=${PASS} pg_dump --host=${HOST} --username=${USERNAME} $DATABASE | gzip > $OUTFILE
