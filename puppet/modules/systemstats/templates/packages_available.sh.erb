#!/bin/bash

COUNTS=`/usr/lib/update-notifier/update-motd-updates-available | sed '/^$/d' | sed 's/[^0-9]//g'`
read -ra AVAILABLE <<< $COUNTS

TOTAL="${AVAILABLE[0]}"
SECURITY="${AVAILABLE[1]}"

sendstat packages.total $TOTAL g
sendstat packages.security $SECURITY g